{
  "name": "fabrics",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Frontend run dev",
    "dev:frontend": "npm --prefix Frontend run dev",
    "dev:backend": "npm --prefix backend start",
    "dev:all": "node scripts/dev-all.mjs",
    "localize:images": "npm --prefix backend run localize:images",
    "localize:images:apply": "npm --prefix backend run localize:images:apply"
  }
}
